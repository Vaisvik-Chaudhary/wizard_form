<div class="container">
<p style="color: green"><%= notice %></p>

<%= render @user %>

<div>
  <%= link_to "Edit", edit_user_path(@user),class:"btn btn-secondary" %> 
  <%= link_to "Back", users_path, class:"btn btn-secondary"%>
</div>
<br>
<div>
  <%= button_to 'Destroy', @user, :method => :delete, :onclick => "return confirm('Are you sure you want to delete this post?')", class:"btn btn-danger" %>

  <%#= button_to 'Destroy', @user, method: :delete, class: "btn btn-danger",data: {confirm: "Are you sure?"}  %>


</div>
</div>
